{
  "name": "PasskeysSignAndVerifyMessage",
  "version": "1.0.0",
  "description": "This example deploys a Safe, sign a message with webAuthn, and verify the message signed",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts"
  },
  "license": "MIT",
  "dependencies": {
    "cbor": "^9.0.2",
    "dotenv": "^16.3.1",
    "ethers": "^6.11.0",
    "typescript": "^5.1.6",
    "abstractionkit": "^0.2.13"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
